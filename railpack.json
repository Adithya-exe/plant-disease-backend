{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": ["git", "git-lfs"],
  "steps": {
    "build": {
      "commands": [
        "git lfs install && git lfs pull",
        "..."
      ]
    }
  }
}
